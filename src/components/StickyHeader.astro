---
import { Image } from 'astro:assets';
import logo from '../assets/tobvision-logo-light.png';
import LanguageSwitcher from './LanguageSwitcher.astro';
import { getContent } from '../content';
import type { Locale } from '../i18n/utils';

interface Props {
  locale: Locale;
  pathname: string;
}

const { locale, pathname } = Astro.props;
const t = getContent(locale);
const homeHref = `/${locale}/`;
---

<header class="sticky-header" aria-label="Seitenkopf">
  <div class="container-content header-inner">
    <a href={homeHref} class="logo-link" aria-label={t.header.logoAlt}>
      <Image
        src={logo}
        alt={t.header.logoAlt}
        widths={[160, 240, 320]}
        sizes="240px"
        loading="eager"
        class="logo-img"
      />
    </a>
    <div class="header-actions">
      <LanguageSwitcher locale={locale} pathname={pathname} />
      <a href={t.header.ctaHref} class="btn-primary header-cta">
        {t.header.ctaLabel}
      </a>
    </div>
  </div>
</header>

<style>
  .sticky-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 40;
    background-color: rgba(20, 20, 20, 0.96);
    backdrop-filter: saturate(130%) blur(6px);
    -webkit-backdrop-filter: saturate(130%) blur(6px);
    border-bottom: 1px solid var(--color-border);
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding-top: 14px;
    padding-bottom: 14px;
  }
  .logo-link {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
  }
  .logo-img {
    height: 40px;
    width: auto;
    display: block;
    filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.4));
    transition: filter 200ms ease, transform 200ms ease;
  }
  @media (min-width: 768px) {
    .logo-img {
      height: 48px;
    }
  }
  .logo-link:hover .logo-img {
    filter: drop-shadow(0 2px 8px rgba(212, 169, 74, 0.25));
    transform: translateY(-1px);
  }
  .header-actions {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .header-cta {
    padding: 10px 18px;
    font-size: 13px;
    min-height: 42px;
  }
  @media (min-width: 768px) {
    .header-cta {
      padding: 12px 22px;
      font-size: 14px;
      min-height: 44px;
    }
  }
  @media (max-width: 520px) {
    .header-cta {
      padding: 10px 14px;
      font-size: 12px;
    }
  }
</style>
